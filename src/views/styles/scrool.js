const element = document.getElementById("content-msj----");

// Crea una función que haga scroll automáticamente
function scrolling() {
  // Establece la posición de scroll en la parte inferior del elemento
  element.scrollTop = element.scrollHeight;

  

}

scrolling()